package org.example.leetcode.stringsCodingPrblems;

public class LongestPrefixSuffixString {
    public static void main(String[] args) {
        String str = "abascfedg";
        int length = findLongestPrefixAndAsSuffix(str);
        System.out.println("length of LongestPrefixAndAsSuffix= " + length);
    }

    private static int findLongestPrefixAndAsSuffix(String str) {
        int right = str.length()-1;
        
        for (int left = 0; left < str.length()-1; left++) {

        }
        return  0;
    }
}
